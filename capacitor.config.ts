import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'who_liked_that',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
